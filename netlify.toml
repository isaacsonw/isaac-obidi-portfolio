# Netlify auto-detects Next.js and applies @netlify/plugin-nextjs (the Next.js
# runtime) automatically — no need to set `publish` or list the plugin here.
[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
